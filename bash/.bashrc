# Подробный вывод содержимого директории 
alias ll="ls -lah --color=auto"

# Git status
alias gs="git status"

# Подняться на уровень выше
alias ..="cd .."

# Греп с подсветкой и номером строки по дефолту
alias grep="grep -n --color=auto"

# Перезагрузить башик
alias src="source ~/.bashrc"